FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm install

COPY . .

EXPOSE 3000

# Port 3000 in-container; map host e.g. 3001:3000 in compose
CMD ["npx", "next", "dev", "-H", "0.0.0.0", "-p", "3000"]
